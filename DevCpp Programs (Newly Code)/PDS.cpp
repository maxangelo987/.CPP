#include <iostream>
#include <cstdlib>
#include <string.h>
#include <conio.h>
#include <stdio.h>
/* run this program using the console pauser or add your own getch, system("pause") or input loop */
using namespace std;

int main(int argc, char *argv[]) {
	
	int i, j, x, y, count, k, l, count1=0;
	char str[100], str1[100];
	char *myword1[20],*myword2[20],*ptr;
	long a,counta,countnaa,num;	

	cout<<"Enter a word:";
	gets(str);
	
	
	scanf(str,"%",&num); //lo -octal, lx- hexa, binary  lp - 16
	cout<<num;


	return 0;
}
